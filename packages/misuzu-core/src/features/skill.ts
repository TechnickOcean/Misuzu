/*
 * implements upon https://agentskills.io
 * ATTENTION: Misuzu do not check the security of the input skill, install with caution.
 */

export interface skill {}
export async function extractSkillFrontmatter() {
  // lenient validation: raise a warning but still load when there's issues
}
export async function importSkillsFromDirectory() {}
export function buildSkillsCatalog() {}
