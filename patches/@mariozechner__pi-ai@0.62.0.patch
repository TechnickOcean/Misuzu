diff --git a/dist/env-api-keys.d.ts b/dist/env-api-keys.d.ts
--- a/dist/env-api-keys.d.ts
+++ b/dist/env-api-keys.d.ts
@@ -1,4 +1,12 @@
 import type { KnownProvider } from "./types.js";
+/**
+ * Register a custom provider -> environment variable mapping for API keys.
+ */
+export declare function registerProviderApiKeyEnv(provider: string, envVarName: string): void;
+/**
+ * Remove a custom provider -> environment variable mapping.
+ */
+export declare function unregisterProviderApiKeyEnv(provider: string): void;
 /**
  * Get API key for provider from known environment variables, e.g. OPENAI_API_KEY.
  *
diff --git a/dist/env-api-keys.js b/dist/env-api-keys.js
--- a/dist/env-api-keys.js
+++ b/dist/env-api-keys.js
@@ -19,6 +19,25 @@
     });
 }
 let cachedVertexAdcCredentialsExists = null;
+const STATIC_ENV_MAP = {
+    openai: "OPENAI_API_KEY",
+    "azure-openai-responses": "AZURE_OPENAI_API_KEY",
+    google: "GEMINI_API_KEY",
+    groq: "GROQ_API_KEY",
+    cerebras: "CEREBRAS_API_KEY",
+    xai: "XAI_API_KEY",
+    openrouter: "OPENROUTER_API_KEY",
+    "vercel-ai-gateway": "AI_GATEWAY_API_KEY",
+    zai: "ZAI_API_KEY",
+    mistral: "MISTRAL_API_KEY",
+    minimax: "MINIMAX_API_KEY",
+    "minimax-cn": "MINIMAX_CN_API_KEY",
+    huggingface: "HF_TOKEN",
+    opencode: "OPENCODE_API_KEY",
+    "opencode-go": "OPENCODE_API_KEY",
+    "kimi-coding": "KIMI_API_KEY",
+};
+const customProviderApiKeyEnvMap = new Map();
 function hasVertexAdcCredentials() {
     if (cachedVertexAdcCredentialsExists === null) {
         // If node modules haven't loaded yet (async import race at startup),
@@ -43,6 +62,18 @@
         }
     }
     return cachedVertexAdcCredentialsExists;
+}
+export function registerProviderApiKeyEnv(provider, envVarName) {
+    if (!provider || typeof provider !== "string") {
+        throw new Error("provider must be a non-empty string");
+    }
+    if (!envVarName || typeof envVarName !== "string") {
+        throw new Error("envVarName must be a non-empty string");
+    }
+    customProviderApiKeyEnvMap.set(provider, envVarName);
+}
+export function unregisterProviderApiKeyEnv(provider) {
+    customProviderApiKeyEnvMap.delete(provider);
 }
 export function getEnvApiKey(provider) {
     // Fall back to environment variables
@@ -83,25 +114,7 @@
             return "<authenticated>";
         }
     }
-    const envMap = {
-        openai: "OPENAI_API_KEY",
-        "azure-openai-responses": "AZURE_OPENAI_API_KEY",
-        google: "GEMINI_API_KEY",
-        groq: "GROQ_API_KEY",
-        cerebras: "CEREBRAS_API_KEY",
-        xai: "XAI_API_KEY",
-        openrouter: "OPENROUTER_API_KEY",
-        "vercel-ai-gateway": "AI_GATEWAY_API_KEY",
-        zai: "ZAI_API_KEY",
-        mistral: "MISTRAL_API_KEY",
-        minimax: "MINIMAX_API_KEY",
-        "minimax-cn": "MINIMAX_CN_API_KEY",
-        huggingface: "HF_TOKEN",
-        opencode: "OPENCODE_API_KEY",
-        "opencode-go": "OPENCODE_API_KEY",
-        "kimi-coding": "KIMI_API_KEY",
-    };
-    const envVar = envMap[provider];
+    const envVar = customProviderApiKeyEnvMap.get(provider) || STATIC_ENV_MAP[provider];
     return envVar ? process.env[envVar] : undefined;
 }
 //# sourceMappingURL=env-api-keys.js.map
diff --git a/dist/models.d.ts b/dist/models.d.ts
--- a/dist/models.d.ts
+++ b/dist/models.d.ts
@@ -3,9 +3,26 @@
 type ModelApi<TProvider extends KnownProvider, TModelId extends keyof (typeof MODELS)[TProvider]> = (typeof MODELS)[TProvider][TModelId] extends {
     api: infer TApi;
 } ? (TApi extends Api ? TApi : never) : never;
+export interface ProxyProviderModelMapping {
+    sourceModelId: string;
+    targetModelId?: string;
+    targetModelName?: string;
+}
+export interface ProxyProviderOptions {
+    provider: string;
+    baseProvider: KnownProvider | string;
+    baseUrl?: string;
+    apiKeyEnvVar?: string;
+    modelIds?: string[];
+    modelMappings?: (string | ProxyProviderModelMapping)[];
+}
 export declare function getModel<TProvider extends KnownProvider, TModelId extends keyof (typeof MODELS)[TProvider]>(provider: TProvider, modelId: TModelId): Model<ModelApi<TProvider, TModelId>>;
 export declare function getProviders(): KnownProvider[];
 export declare function getModels<TProvider extends KnownProvider>(provider: TProvider): Model<ModelApi<TProvider, keyof (typeof MODELS)[TProvider]>>[];
+/**
+ * Register a proxy provider by cloning models from an existing provider.
+ */
+export declare function registerProxyProvider(options: ProxyProviderOptions): Model<Api>[];
 export declare function calculateCost<TApi extends Api>(model: Model<TApi>, usage: Usage): Usage["cost"];
 /**
  * Check if a model supports xhigh thinking level.
diff --git a/dist/models.js b/dist/models.js
--- a/dist/models.js
+++ b/dist/models.js
@@ -1,4 +1,5 @@
 import { MODELS } from "./models.generated.js";
+import { registerProviderApiKeyEnv } from "./env-api-keys.js";
 const modelRegistry = new Map();
 // Initialize registry from MODELS on module load
 for (const [provider, models] of Object.entries(MODELS)) {
@@ -18,6 +19,87 @@
 export function getModels(provider) {
     const models = modelRegistry.get(provider);
     return models ? Array.from(models.values()) : [];
+}
+function cloneModel(source, overrides) {
+    const next = {
+        ...source,
+        ...overrides,
+        input: overrides?.input ? [...overrides.input] : [...source.input],
+        cost: {
+            ...source.cost,
+            ...(overrides?.cost || {}),
+        },
+    };
+    if (!next.compat && source.compat) {
+        next.compat = { ...source.compat };
+    }
+    return next;
+}
+function resolveProxySourceModelIds(baseProviderModels, options) {
+    if (options.modelMappings && options.modelMappings.length > 0) {
+        return options.modelMappings.map((mapping) => {
+            if (typeof mapping === "string") {
+                return {
+                    sourceModelId: mapping,
+                    targetModelId: mapping,
+                    targetModelName: undefined,
+                };
+            }
+            return {
+                sourceModelId: mapping.sourceModelId,
+                targetModelId: mapping.targetModelId || mapping.sourceModelId,
+                targetModelName: mapping.targetModelName,
+            };
+        });
+    }
+    const modelIds = options.modelIds && options.modelIds.length > 0
+        ? options.modelIds
+        : Array.from(baseProviderModels.keys());
+    return modelIds.map((modelId) => ({
+        sourceModelId: modelId,
+        targetModelId: modelId,
+        targetModelName: undefined,
+    }));
+}
+/**
+ * Register a proxy provider by cloning models from an existing provider.
+ *
+ * This is useful for OpenAI-compatible proxy services where you want to:
+ * - keep the same API behavior as an existing provider
+ * - expose it under a new provider name
+ * - control which environment variable is used for API keys
+ */
+export function registerProxyProvider(options) {
+    if (!options.provider || typeof options.provider !== "string") {
+        throw new Error("provider must be a non-empty string");
+    }
+    if (!options.baseProvider || typeof options.baseProvider !== "string") {
+        throw new Error("baseProvider must be a non-empty string");
+    }
+    const baseProviderModels = modelRegistry.get(options.baseProvider);
+    if (!baseProviderModels) {
+        throw new Error(`Unknown base provider: ${options.baseProvider}`);
+    }
+    const mappings = resolveProxySourceModelIds(baseProviderModels, options);
+    const proxyModels = new Map();
+    for (const mapping of mappings) {
+        const sourceModel = baseProviderModels.get(mapping.sourceModelId);
+        if (!sourceModel) {
+            throw new Error(`Unknown model '${mapping.sourceModelId}' for provider '${options.baseProvider}'`);
+        }
+        const targetModel = cloneModel(sourceModel, {
+            id: mapping.targetModelId,
+            name: mapping.targetModelName || sourceModel.name,
+            provider: options.provider,
+            baseUrl: options.baseUrl || sourceModel.baseUrl,
+        });
+        proxyModels.set(targetModel.id, targetModel);
+    }
+    modelRegistry.set(options.provider, proxyModels);
+    if (options.apiKeyEnvVar) {
+        registerProviderApiKeyEnv(options.provider, options.apiKeyEnvVar);
+    }
+    return Array.from(proxyModels.values());
 }
 export function calculateCost(model, usage) {
     usage.cost.input = (model.cost.input / 1000000) * usage.input;
